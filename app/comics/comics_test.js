'use strict';

describe('rbMarvel.comics module', function() {

  beforeEach(module('rbMarvel.creators'));

  describe('comics controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var comicsCtrl = $controller('ComicsCtrl');
      expect(comicsCtrl).toBeDefined();
    }));

  });
});