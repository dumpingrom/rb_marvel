'use strict';

describe('rbMarvel.creators module', function() {

  beforeEach(module('rbMarvel.creators'));

  describe('view2 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var creatorsCtrl = $controller('CreatorsCtrl');
      expect(creatorsCtrl).toBeDefined();
    }));

  });
});